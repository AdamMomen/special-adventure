# Add Python and build tools for node-gyp (required by usb, tiny-secp256k1)
# These packages are transitive deps of @solana/wallet-adapter-wallets
[phases.setup]
aptPkgs = ["...", "python3", "build-essential"]
